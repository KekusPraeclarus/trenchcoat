---
description: What must always be true in trenchcoat - isolation, prompt-injection resistance, broadcast egress, alpha-queue lifecycle, rate limits, state auditability. Falsifiable properties with stable IDs.
scope: project
status: active
last_verified: 2026-08-31
read_when:
  - Before editing the sandbox config, snapshot pipeline, agent prompts, collectors, or watchlist state handling.
---

# Invariants

What must always be true in this system, regardless of input, ordering, or caller.
Each row is a falsifiable property with a stable ID that tests, reviews, and commits
cite. When code and this file disagree, one of them is wrong — fix whichever it is
in the same change.

Many rows remain `GAP`/`PARTIAL` while offline modules land ahead of live
canaries and remaining post-run checks. A row flips to `ENFORCED` only when its
check exists at the cited site and covers the full claim.

## Status legend

| Status | Meaning |
|--------|---------|
| ENFORCED | A check exists at the cited location |
| STRUCTURAL | Impossible by construction (types, derivation, runtime rules) |
| PARTIAL | Enforced with a known gap (cite the finding) |
| GAP | Not enforced yet (cite the finding) |

## I — Isolation

| ID | Invariant | Status | Verification |
|----|-----------|--------|--------------|
| INV-I1 | The runtime agent process cannot read or write any path outside `agent/`, ever | PARTIAL | Write confinement ENFORCED under host Cursor CLI `--sandbox enabled` + `agent/.cursor/sandbox.json` (`disableTmpWrite: true`); live escape probe (`TRENCHCOAT_LIVE_ISOLATION=1`) proves outside writes fail on a non-tmp layout. Outside **reads** still succeed on current CLI — keep secrets out of child env (`scrubChildEnv`) and never under `agent/`. Docker runner is reference-only. **Documented exception (ADR 040):** allowlisted Telegram `/agent` turns run one-off with `--sandbox disabled` + `--force` against `TRENCHCOAT_REPO_ROOT` and may read/write outside `agent/` |
| INV-I2 | The runtime agent process has no network access | PARTIAL | sandbox `networkPolicy` deny-all; host-CLI network-deny probe green 2026-07-18 (`TRENCHCOAT_LIVE_ISOLATION=1`, `tests/sandbox/agent-network-deny.test.ts`); stays PARTIAL because Docker compose `network_mode: none` is reference-only and not the production boundary. **Documented exception (ADR 040):** Telegram `/agent` turns disable the sandbox and therefore may reach the network |
| INV-I3 | No credential (router/Telegram/provider keys, Twitter session material, or Cursor API keys if ever used) exists under `agent/` at any time; Cursor auth for jobs is host CLI login, never copied into the workspace | PARTIAL | gitleaks (`pnpm secret-scan`, `.github/workflows/gitleaks.yml`) plus mutation-lane `secret-scan` (`gitleaks dir`, assignment-regex fallback) + snapshot writer never receives env values; session argv built without embedding secrets (production launchers assert no `CURSOR_API_KEY` argv, `tests/unit/session.test.ts`); `scrubChildEnv` / `prop_inv_i3_scrub_*` drops router/Telegram/provider keys from the Cursor child env — Twitter session material remains outside `agent/` by design |
| INV-I4 | Only the snapshot writer creates files under `agent/inbox/`, and it rejects any path resolving outside it (`..`, absolute, symlink) | PARTIAL | path guard + `prop_inv_i4_*` on `src/lib/snapshot.ts`; static ownership proof that only `SnapshotWriter` implements writes and callers are the allowlisted orchestrator collectors (`collect.ts`, `research-collect.ts`, `research.ts`, `research-candidates.ts`, `new-pools-feed.ts`, `chart-collect.ts`, `narrative-collect.ts`, `watchlist-collect.ts`, `review-collect.ts`, `x-fyp-eligible.ts`, `pump-collect.ts`, `pump-fyp-eligible.ts`, fomo collectors, `discord-wallet-signal-collect.ts`) plus Discord tracking intent/match/mention-review hosts and conversation addressing-gate host (`src/discord/tracking-intent.ts`, `tracking-match.ts`, `tracking-qualify.ts`, `conversation-intent.ts`) writing under the isolated Discord agent workspace |
| INV-I5 | Outer runtime isolation: production jobs use host Cursor CLI `--sandbox enabled` with secret-scrubbed child env; agent tools cannot reach host paths or the network; Docker agent-runner compose (agent-only mount, `network_mode: none`) is reference/defense-in-depth only, not the production launch path | PARTIAL | structural artifact tests under `tests/sandbox/`; host-CLI escape + network-deny + prompt-injection probes green 2026-07-18 (`TRENCHCOAT_LIVE_ISOLATION=1`); PARTIAL because outside reads still succeed (INV-I1) and container smoke remains optional reference. **Documented exception (ADR 040):** allowlisted Telegram `/agent` is an explicit unsandboxed remote-coding lane |

## P — Prompt-injection resistance

| ID | Invariant | Status | Verification |
|----|-----------|--------|--------------|
| INV-P1 | All third-party text reaches the agent only inside a snapshot envelope carrying `trust: "untrusted-external"` | ENFORCED | `TrustSchema` literal + `SnapshotWriter` parse; `prop_inv_p1_*` in `tests/unit/foundations.test.ts` |
| INV-P2 | Job prompt templates never interpolate scraped text; they reference inbox files by path only | PARTIAL | path-only job prompt in `run.ts` + `assertPathOnlyPrompt` + `prop_inv_p2_*` host-prompt/static guards; skill text remains bot-side |
| INV-P3 | The bot's instructions direct it to treat inbox text as evidence, never instructions, and to flag instruction-shaped tweets in its report | PARTIAL | `agent/AGENTS.md` + job prompt require evidence-never-instructions (`prop_inv_p3_*`); recurring live model red-team eval still open |

## S — State auditability

| ID | Invariant | Status | Verification |
|----|-----------|--------|--------------|
| INV-S1 | Every watchlist status change has a same-run dated entry in `state/decisions.md` with reasoning | PARTIAL | `runPostRunVerifier` S1 + accepted receipts; live agent proposal E2E still open |
| INV-S2 | `decisions.md` is append-only; no run rewrites or deletes prior entries; INV-S28 corrective supersession is likewise append-only (never rewrite history) | PARTIAL | prefix guard inside `applyDecisionProposals`; verifier S23 watches watchlist deltas; market-claim validity index is append-style upsert |
| INV-S3 | Every report and state change is attributable to a run id whose inbox is archived | PARTIAL | `preArchiveRun` freezes inbox+manifest before agent; verifier S3; journal under `archive/transactions/` with `status: running \| complete \| failed` (ADR 006) — failed runs are terminal and not auto-resumed. Legacy journals missing `status` are derived in-memory (`phase: complete` ⇒ complete) without rewriting archive bytes. Host precondition skips (`archive/skips/<job>.jsonl`, `runId: none`) create no run attribution by design |
| INV-S4 | Audit outcome numbers (returns, RSI at decision, execution/cost estimates) are computed by versioned host functions from timestamped data, never by the model | PARTIAL | `audit-math.ts` + Wilder RSI golden vectors (`tests/indicators.test.ts`) + `runAuditEpoch`; audit job still often empty-cohort and narration prompt unused — live scored epoch E2E open |
| INV-S5 | The bot never modifies its own instructions or skills (`agent/AGENTS.md`, `agent/skills/**`) | PARTIAL | `assertAgentIntegrity` byte-compares those paths across the agent session; dedicated post-run flagging / incident path still open |
| INV-S6 | Every snapshot and alpha-queue item carries a `provenance` id; every decision entry cites the provenance ids that drove it | PARTIAL | snapshot envelopes require `provenance` (schema); proposals require `provenanceIds` for track/drop; verifier `checkS6` (`src/orchestrator/verify.ts`) rejects any accepted receipt citing provenance absent from the frozen archived-inbox allowlist, and fails closed when the allowlist is empty but a citation exists; the run halts before seal on failure. **Gap:** `formatDecisionMarkdown` (`src/orchestrator/proposals.ts`) writes thesis/drivers/sources into `decisions.md` and omits `provenanceIds` — receipt/archive path covers citations, the decision entry does not |
| INV-S7 | `state/sources.json` is written only by deterministic host code (audit scoring maths, rug-shill dock, operator undock/confirm, neutral auto-registration) — never by any model session | PARTIAL | integrity snapshot + `SourceWriter` sole `saveSources` owner (`sources-write.ts`); full audit scoring loop still open |
| INV-S8 | Every completed run seals fsynced archive journal/receipts before alpha purge, external delivery, or the completed marker; retries resume from the journal and never duplicate a keyed side effect; remediation post-fix audit resumes from durable phases/side effects (INV-S28) | PARTIAL | archive-sealed side effect + journal-store fsync (ADR 006); verifier fail-closed before purge/egress; remediation journals + correction eventId idempotency; phase resume across process restarts still thin |
| INV-S9 | A token hard-failing the security gate (mapping in security-gate.md), on an unsupported chain, without a bound canonical `(chain, token_address, pair_address)` identity (`resolved` or dossier-validated `model-confirmed`, token-resolution.md), or failing the contextual mint rule (active `mintable`/`mint-authority` caution + model `memecoin` classification or missing `projectClassification`) can never enter `watchlist.json` as `tracking`. Active watch needs market-quality evidence: `tracking` requires security pass and market-quality pass; `watching` is allowed only with security pass and explicit market-quality fail (host auto-downgrades `tracking`→`watching`; no ledger). Missing market-quality evidence blocks `tracking` and `watching` | PARTIAL | `resolveGateArchiveThenLive` + archive-only `resolveMarketQualityFromArchive` (inbox `market-quality` dossier; no live MQ refetch) + host downgrade on MQ fail (no ledger; research broadcast blocked via `market-quality-watching`) + `securityThresholdsFromConfig` + `mintTrackBlockReason` / `evaluateResearchSubscribe`; operator research uses the same gate + MQ + provenance allowlist as scheduled runs; mint blocks only final `tracking`; verifier S9 (tracking needs MQ pass, watching needs MQ fail, missing MQ receipt fails); live E2E still gated |
| INV-S10 | `state/ledger.json` and `state/research-queue.json` are written only by deterministic orchestrator code: every track-call opens exactly one entry-pending position, finalised at the first eligible post-decision observation (`settle-ledger` via `outcomes-settle`); drop on `entry-pending` cancels (`censored`); drop on `open` marks exit-pending; queue entries are enqueued/dequeued by the run loop, `runOperatorResearchNow`, or host-only bridges (`narrative-bridge`, `social-cashtag-bridge`, `new-pools-enqueue`, and other allowlisted host enqueue paths) after integrity passes; no model session writes either file | PARTIAL | integrity snapshot byte-compares both files across the agent session; unit tests `prop_inv_s10_*` + `settle-ledger.test.ts`; [ADR 031](adr/031-wallet-settle-brief-locks-and-ledger.md), [ADR 046](adr/046-earlier-token-discovery.md); exit-bar finalisation + audit scorecard persist still open |
| INV-S11 | Recovery never expands privileges or destroys committed history: rollback targets only the last completed archive seal, incomplete post-seal runs resume deterministically, pre-seal SIGTERM orphans are marked `failed` (not auto-resumed), conflicts quarantine, and INV-S2/S7/S10 hold during recovery | PARTIAL | quarantine + `findIncompleteRuns` + `abandonOrphanedRuns` / `maybeAbandonOrphansThrottled` in `runJob` / `signal-interrupted` on SIGTERM; full rollback ladder still open |
| INV-S12 | No model-authored artifact participates in any `sources.json` write, with one bounded exception: the intent classifier's verdict enters as a constrained enum that can only attenuate a dock already decided deterministically. Dock attribution is host-side contract/pair matching over pre-session snapshots; quality scoring uses only direct host-extracted bullish call events from those snapshots; dock triggers come only from typed scanner responses — never from queue entries, decisions.md citations, or workspace files | PARTIAL | call-log from archived inbox + `rug-dock` / `SourceWriter`; live scanner dock loop still open |
| INV-S13 | The intent classifier is leniency-bounded and fail-closed: isolated session (no tools, no workspace state, fixed host-side prompt), output parsed strictly as `shill` \| `warn` with any other output treated as `shill`; a `warn` suspends the immediate penalty but never writes scores, DMs an exoneration proposal to the allowlisted operator via the chat bot for manual undock/confirm, and the rug-adjacency counter increments regardless of verdict | PARTIAL | `intent-session` + `exoneration` + CLI/chat `undock`/`confirm`; live classifier session still optional |
| INV-S14 | Audits are leakage-free and feedback-lagged: the as-of bundle is the sole evidence-time record; execution and outcome observations are immutable post-event records; evidence weighting uses start-of-run source-score snapshots; source-score epochs cover only call events before the previous score cutoff | PARTIAL | `writeDecisionBundle` on proposal commit + epoch freeze/seal + lagged source outcomes; RSI-rich indicator freeze still thin (card/dex signals first) |
| INV-S15 | One state writer at a time for **agent workspace** mutations: continuous scan/research jobs that need long critical sections hold `agent/.lock` for their full duration; lock-free chat reads never write. **Exempt (own confinement / brief RMW, ADR 027 + ADR 031):** `harness-improve`, `harness-meta-improve`, `incident-remediate`, `incident-remediate-weekly`, `outcomes-settle`, `wallet-scan-solana`, `wallet-scan-evm`, and `wallet-review` do **not** take a full-job `agent/.lock` — improvement lanes use remediations/harness/repo-mutation locks; wallet settle/scan/review run provider I/O unlocked and take a brief `withAgentWorkspaceLock` only for `wallets.json` / ledger RMW so scans cannot starve settlement. If that brief lock is still held after settle retries, ledger/Fomo score writes stay pending (`lockDeferred`) and the settle journal may still complete. **`outcomes-settle` additionally holds a job mutex** (`~/.trenchcoat/locks/outcomes-settle.lock`) so timer/manual kicks cannot stack; orphan abandon and health `stuck-incomplete-run` skip the 30m / 2h rules for that job and use a 24h hard age cap | PARTIAL | `jobRequiresAgentWorkspaceLock` / `AGENT_LOCK_EXEMPT_JOBS` / `JOB_MUTEX_JOBS`; `WorkspaceLock` in `runJob` for non-exempt jobs + job mutex for `outcomes-settle`; remediations/ harness locks; `withAgentWorkspaceLockOrDefer` for settle RMW; `incompleteRunStuckMs` in `health.ts`; [ADR 027](adr/027-improvement-lanes-skip-agent-lock.md), [ADR 031](adr/031-wallet-settle-brief-locks-and-ledger.md); post-audit service kick keeps audit/`agent/.lock` separate from lock-exempt harness (`ops/install-*.sh`) |
| INV-S16 | Model-judged token disambiguation is shortlist-bounded and security-inert: a confirmed binding must be one of the dossier's deterministically-collected candidates (orchestrator-validated before binding), and `model-confirmed` identities never participate in rug-dock attribution or any `sources.json` write — dock attribution matches raw CAs only (per INV-S12) | PARTIAL | `validateModelPick` + `rug-dock` excludes model-confirmed; orchestrator binding still open |
| INV-S17 | RSI is reproducible and causally aligned: one versioned Wilder implementation consumes only contiguous, closed, event-time candles; every value carries its exact input hash/bar cutoff; missing or partial inputs invalidate the feature; no RSI rule is promoted from proxy labels or evaluation data it was tuned on | PARTIAL | Wilder RSI + golden vectors + closed-candle filters; named promotion-gate tests still open |
| INV-S18 | Audit epochs and outcome missingness are tamper-evident and idempotent: a frozen cutoff and cohort define each epoch; sealed reruns are byte-identical; missing pools/provider data are never silently converted to losses or exclusions; every aggregate exposes numerator, denominator, and reasoned exclusions | PARTIAL | `materializeObservation` pending/censored never invents losses; `sealEpoch` / settlers; crash/outage fixtures still open |
| INV-S19 | Wallet state is host-only: hard exclusions are absolute; `blended = 0.80*det + 0.20*llm` with fail-closed neutral 50; causal finalized **verified swap-buy / swap-sell** outcomes only (target-token gain or decrease + native/allowlisted quote spend or receive; block/slot timestamps; stable `providerEventId`); wallet quality scores lagged **FIFO copy-trade `realizedReturn`** (buy→sell; open buys unsettled); reorg/`removed` logs invalidate unfinalized rows; runtime agents cannot nominate/score/add/drop wallets; Fomo never registers wallet candidates (profile addresses are not trading wallets) and legacy `discoveredFrom: "fomo"` records are quarantined — Fomo **feed** trades score separately in `fomo-trader-scores.json` from `settlementStatus: priced` FIFO closes only (`sell-only`, `non-priceable`, and `provider-pending` never invent losses); Pump.fun profile ids never enter `wallets.json`; Discord wallet-signal channel actors (ADR 035) never write `wallets.json` and are not smart-wallet identities; convergence uses event-time `tracking` **buys** only | PARTIAL | integrity byte-protects `wallets.json` + `wallet-runners.json`; discovery/scan/runner-discovery host phases are sole state/cursor writers; `prop_inv_s19_*` + wallet copy-trade settlement + fomo-origin and pump-origin static confinement; live provider E2E still gated |
| INV-S20 | Every wallet add/drop is an immutable, idempotent transition that emits exactly one durable `wallet.lifecycle` router event with a host-rendered one-liner on a lane that does not consume market broadcast budget; tracked-wallet convergence emits typed `wallet.convergence` events (host-rendered `UNVERIFIED WALLET CONVERGENCE`, capped, canary-blocked) that never mutate watchlist/wallet lifecycle | PARTIAL | `wallet-review` / operator seed / `wallet-convergence` stage via Outbox unless canary `blockExternalEffects`; lifecycle + convergence line + budget independence tests |
| INV-S21 | Source-list membership is decided only by deterministic host code from lagged, settled, direct bullish raw-CA outcomes. Headline metric is **peak% from entry** (quiet 6h / 14d force-settle; hit ≥ +20%). FYP text, model artifacts, engagement, follower counts, and same-epoch outcomes cannot promote an account. Every transition is immutable and idempotent; retries cannot produce duplicate logical membership changes. Fomo classifications never shortcut into managed-list membership; `fomo-leaderboard` candidates require sealed historical X-post call outcomes plus the usual one-epoch lag. Shiller entry uses sealed X-post CAs only (10 calls / 5 tokens). FOMO profile buys do not count. Promotion scores X-post CAs from the call log. FOMO traders score on FIFO `fomo-trader-scores.json`. FOMO platform follows are host-only feed training and never write the X list or `wallets.json`. FOMO profile wallets never enter `wallets.json` | PARTIAL | lifecycle engine + `outcomes-settle` / `settle-source-peaks` with live DexScreener→GeckoTerminal→SolanaTracker→Birdeye `BarProvider`s on Solana (empty ⇒ pending, never invented loss); Fomo merge fail-closes on unsealed evidence IDs (`fomo-x-classification-merge`); live pricing E2E still gated |
| INV-S22 | The runtime agent owns FYP like/follow/unfollow choices for narrative/sentiment feed training. Engagement never writes managed-list membership or source scores. Likes are hard-capped at 2 per 10 minutes; like/follow/unfollow targets must appear in the same-run FYP snapshot; posts, replies, DMs, and retweets remain blocked. Fomo narrative follows are host-gated after 14-day utility probation and never come from classification alone; historical X posts tagged for source evaluation never enter live narrative scans | PARTIAL | schema hard bounds + `inbox/<run-id>/x-fyp-eligible.json` manifest + FYP post/author binding in `src/social/x-engagement.ts` + proposal-time subscription-state dedupe (`already_liked` / `already_following` / `not_following` / `pending_duplicate`, runId-independent, no daily bump) + hardened primaryColumn-scoped follow/unfollow selectors with bounded hydration retry and typed `account_not_followable` (`src/collectors/twitter/engagement.ts`) + `state/x-bot-health.json` + narrative probation state; live 2026-07-18: likes on `list-scan-2026-07-18T18-03-03-175Z`, follow on `list-scan-2026-07-18T18-36-02-564Z` (health `consecutiveFailures=0`); some profiles may still surface `account_not_followable` / pending duplicates |
| INV-S30 | The pump-scan agent owns like/follow/unfollow for Pump.fun feed training. Targets must appear in the same-run `pump-fyp-eligible` snapshot. Likes cap at 2 per 10 minutes. Follows cap at `max_follows_per_run`. Engagement never writes caller scores, research-queue, watchlist, or wallets. Pump calls settle in `archive/outcomes/pump-call-*.json` only | PARTIAL | `applyPumpEngagementChoices` bind + integrity on `pump-engagement.json` / `pump-caller-scores.json` / `pump-bot-health.json`; `tests/redteam/pump-engagement-bind.test.ts`; live Pump canary still gated |
| INV-S31 | FOMO platform follows are host-only. They train the FOMO feed so the host can see followed-trader buys. Follows never write `wallets.json`, the X managed list, or shiller scores. X review nominates only an explicit FOMO profile X link. Same-handle fallback does not enter pending. Classification uses X posts only. Shiller entry is X-post CAs only. Narrative follows stay on the X engagement path after probation | PARTIAL | `fomo-follows.json` integrity + trader-sync follow planner + `requireProfileLink` upsert + X-only merge tests |
| INV-S23 | Watchlist, ledger, and `decisions.md` mutations come only from host-validated decision proposals; model sessions write proposals under `reports/<run-id>/` and never mutate those state files directly | PARTIAL | `proposals.ts` + verifier S23 + fail-closed halt before seal when checks fail; `state/INDEX.md` and `state/narratives/log.jsonl` are host-owned (integrity-protected, `AGENT_PROTECTED_PATHS` in `src/orchestrator/integrity.ts`) — the agent proposes narrative updates only in `reports/<run-id>/narrative-proposals.jsonl` and the host schema-validates + merges (`mergeNarrativeProposals`) after the session, including optional monotonic `framing` / `framingMaturedAt` / `framingEvidence` (ADR 036); INDEX reconciled after watchlist mutations, narrative prune, and `tc watchlist remove` |
| INV-S24 | Harness improvement patches are confined to the literal allowlists `agent/skills/decision-policy/policy.json` (policy lane) and `config/harness-improver.json` (meta lane, ADR 039). Policy candidates are evaluated with worktree `test:all` + single-use holdout replay + protected-metric gates. Meta candidates are shadow-only until operator promotion after ≥8 paired offline trials under host-owned meta-utility. Neither lane may rewrite audit definitions, router, chat, harness (`src/harness/**` code/gates), contracts, host gate prompts, holdout/meta-trial registries, secrets, or allowlist constants (ADR 038/039). Propose/mining/keep/prior-attempt inputs are sealed aggregates, decision metadata/enums, numeric signals, host gate flags, and settled numeric outcomes only — never inbox/scraped/card prose. Operator feedback enters the harness only as one sealed numeric preference set (`sealed/active-preference-set.json`, ADR 043): `src/harness/**` never imports the live feedback store and never reads operator prose, tags, or ledger lines; a later policy candidate that lowers preference agreement is rejected as `operator-preference-regression`. Prior-attempt indexes are host-built and rebuildable from rejection/rollback receipts. Scheduled jobs may plan/review/build and, after independent agent approval, fast-forward `origin/main` then local `main` (`push_origin`, default on; kill switch `false`); they never rewrite history, never waive deterministic gates, and never activate `~/.trenchcoat/agent` while the all-work drain gate is busy. Runtime redeploy (`ops/install-launchd.sh` / `ops/install-systemd.sh`) sets deploy-pause (defers new jobs), waits for `isAgentIdle` (after orphan abandon), reloads schedulers, then clears pause and kickstarts deferred jobs so in-flight work is not killed mid-session and paused work resumes. **Failsafe:** install EXIT trap restores stopped timers/jobs if the install aborts; `deploy-pause.json` older than 45m is treated as cleared (`DEPLOY_PAUSE_MAX_AGE_MS`); Deploy VPS SSH `command_timeout` must outlive wait-idle. Integrate/publish acquires `~/.trenchcoat/repo-mutation.lock` so it cannot race chain-integration or incident-remediation publish. **Unchanged by INV-S28:** post-fix claim audit / `finding.correction` must not enter the policy harness or canary lane | PARTIAL | `confineDiff` + POLICY_ALLOWLIST + IMPROVER_CONFIG_ALLOWLIST + plan/review/build agents + holdout registry + meta paired trials + `evaluate.ts` protected metrics + integrate push+ff/deploy/drain + `waitForAgentIdle` + `deploy-pause.json` TTL + install abort restore + `repo-mutation.lock` + `assessHarnessImproveReadiness` read-only preflight (consumed holdout + sample floors) before propose; typed skips → schedule-report + skip ledger; [ADR 038](adr/038-improver-self-edit-boundary.md), [ADR 039](adr/039-bounded-improver-config-lane.md); live Cursor sessions injectable and gated |
| INV-S25 | Active canaries may assign a configured share of internal decision episodes to a candidate policy, but candidate external effects (broadcast, router, X mutations, wallet lifecycle) are always blocked; rollback only changes future assignment to baseline (append-only history) and may revert via a normal commit + runtime redeploy. Publish/redeploy paths that mutate the repo share `repo-mutation.lock` with other mutation lanes. **Unchanged by INV-S28:** corrections are remediation integrity notices, never canary-assigned policy experiments | PARTIAL | `blockExternalEffects` covers proposals, wallet paths, X engagement, and skips outbox budget/delivery; `recordPairedEpisode` + `maybeBumpCanaryMatureCounts` wired on candidate assignment |

## B — Broadcast and chat egress

| ID | Invariant | Status | Verification |
|----|-----------|--------|--------------|
| INV-B1 | Only the orchestrator stages events into the router; the sandboxed agent has no path to it (follows from INV-I2). Host remediation may stage `finding.correction` integrity notices after INV-S28 gates | PARTIAL | router URL/auth/HMAC only in orchestrator env; `scrubChildEnv` drops secrets from Cursor child; HMAC client solely in `src/orchestrator/router.ts`; host-CLI network-deny probe green 2026-07-18 (INV-I2) — PARTIAL while INV-I2 remains PARTIAL |
| INV-B2 | Every broadcast is schema-valid (`severity`, length-capped `text`, frozen `refs`, typed host-verifiable `audit_claim`) and host-worthiness-approved before stage; failures and rejects are receipted, never silently dropped. Worthiness receives trusted recent accepted **and still-staged** same-subject broadcasts (48h) and may not infer prior delivery from narrative state or untrusted notes; reworded same-catalyst duplicates are rejected, genuinely new same-subject developments remain eligible. New slugs, stage changes, notable concrete same-stage developments, and founder/protocol primary-source catalysts (material product/wallet/protocol/ecosystem announcements from founder, CEO, protocol official, or official project channel — no CT-cluster or stage-shift prerequisite) may broadcast; exact/recent update repeats and pure status-quo stage restatements are rejected. Matured narrative framing (`ecosystem`/`regime` on `state/narratives/log.jsonl`) forbids lane-“rotation” wording in outbox/distill copy — host rejects `stale-narrative-framing` (display framing ≠ capital-flow claim type `rotation`; ADR 036). Completed resolved research with a clear trade, watch, or avoid conclusion proposes one positive or negative broadcast. Thin `watching` tokens from host market-quality fail block research broadcasts with `market-quality-watching`. Worthiness input is claim, refs, untrusted candidate proposal text, 48h histories, and — when `broadcast.feedback.enabled` — bounded liked/disliked examples from delivered broadcasts (`up`/`down`/`ambiguous` only; `retracted` and no reaction excluded); a 48h `{subject,claimHash}` cache may stage or reject without a new LLM call for token and wallet claims only (ADR 034). Open narrative claim types always re-run worthiness. Host rejects a rewrite that shares a name cluster with a 48h same-subject accepted broadcast (`mechanical-same-catalyst` / `development-same-catalyst`). Outbox envelopes are capped at eight items per run. Intraday Telegram is one short topic paragraph per normalized subject per run (≤800 Markdown chars; no section briefings); followers omit `channels.telegram`. Discord receives the same rendered text as Telegram leaders; topic-merged followers omit both. Daily `narrative.digest` (04:00 Europe/London, Telegram-only) covers retention-active narratives with a host-approved Telegram development in the window. Distiller aims for an assembled map near 8000 characters. Longer maps still stage and send via section-aware chunks. A raw `.md` file goes only to the operator interface bot, never the public channel. Transport bound is `ROUTER_EVENT_TEXT_MAX`. Immutable per-London-date ledger. Quiet actives omitted. Length is never an editorial drop. Delivery is durable and idempotent, with bounded host retry. Channel text remains fail-closed, mention-safe, and host-rendered from agent-authored broadcast copy or host-bounded packets; the host never invents market text. Public channel distill output must not leak internal jargon (`tape`, `operator`, `lane noise`), use watch/ignore checklist framing, or use the stock closer `worth watching` — host rejects `internal-jargon` / `stock-watch-phrase` and applies `PUBLIC_COPY_RULES` (ADR 037). Narrative claim types (`narrative-emergence`, `narrative-fade`, `narrative-development`, `rotation`, `sentiment-collapse`) additionally need curated social evidence at tier `strong` — at least `min_fresh_posts` eligible posts, at least `min_independent_authors` authors, and a promotional share at or below `max_promotional_share`; host rejects `narrative-evidence-quality:<floor>` (ADR 042). Curation shapes only the derived `narrative-social-*` snapshots; sealed collector archives stay byte-identical. Worthiness and copy prompts may also carry operator-approved guidance lines from `config/broadcast-output-tuning.json` only — short bounded host text written by `broadcast feedback apply`; raw operator prose never enters a prompt (ADR 043). `finding.correction` retains its INV-S28 bypass and delivery integrity rules | PARTIAL | `ingestOutbox` + reject receipts + `social-evidence` / `narrative-evidence-gate` (ADR 042) + `broadcast-worthiness` + `worthiness-examples` (ADR 014/023/024/026/034/036/037/038/043) + worthiness cache (`worthinessCacheApplies`) + mechanical gate (`market-quality-watching` / `mechanical-same-catalyst`) + `narrative-stage-dedupe` / `narrative-development` (`development-same-catalyst`) + `usesStaleRotationFraming` / `stale-narrative-framing` + `INTERNAL_JARGON` / `internal-jargon` + `STOCK_WATCH_PHRASE` / `stock-watch-phrase` + topic grouping in `renderChannelPayloads` + `telegram-digest` ledger + digest `.md` fanout + canonical refs + research outbox contract + durable router delivery/retry + provider message-id receipts |
| INV-B3 | Chat replies go only to allowlisted Telegram user ids. Broadcast feedback detail follows the same path: the host asks only the allowlisted operator, binds a reply to one open request (a direct reply wins; a bare reply binds only when exactly one request is open), writes the raw text to one confined `untrusted-external` evidence file, and stores only bounded tags plus one derived summary (ADR 043) | PARTIAL | allowlist gate + `replyChatId` (operator id) in `src/chat/handler.ts`; listener drops non-private chats; `bindFeedbackReply` + `writeFollowupEvidence` + `classifyFollowupReply`; unit tests cover spoofed ids and reply targeting; host alert/exoneration DMs still need the same reply-target discipline wherever they are wired |
| INV-B4 | Discord market broadcasts use the same host render and fanout rules as Telegram (no separate daily message budget or run-scoped dedupe); topic-merged followers omit both destinations | PARTIAL | unified fanout in `renderChannelPayloads` (ADR 041); router `skipped-no-channel-payload` when a destination payload is absent |
| INV-B5 | Router HMAC rejects replayed nonces, skewed timestamps, and bad signatures; exact event duplicates return success without re-queue; eventId/payload conflicts return 409 and incident logs; fanout is durable at-least-once with dead-letter visibility; `finding.correction` uses the same HMAC/dedupe contract with stable incident-scoped eventIds | ENFORCED | `src/lib/router-contract.ts`, `src/router/accept.ts`, `src/router/deliver.ts`, orchestrator HMAC client in `src/orchestrator/router.ts`; `tests/contract/router.test.ts`, `tests/unit/router.test.ts`, `tests/integration/router-delivery.test.ts` |
| INV-B6 | Broadcast feedback is operator-scoped and bounded: only `👍`/`👎` from `DISCORD_OPERATOR_USER_ID` in `broadcast.feedback.channel_id` count, every other reaction is dropped with a typed reason, and a reaction resolves to a delivered broadcast through `provider_message_index` only. Both marks record `ambiguous`; no mark records `retracted`. The ledger is append-only and idempotent — any message part of one broadcast maps to one `feedbackId`, and a repeated reaction set changes nothing. `down` and `ambiguous` open exactly one Telegram detail request with a 72h expiry. Live reactions load delivered broadcast text into the host worthiness gate as liked/disliked examples (`up` lean approve, `down`/`ambiguous` lean reject, `retracted`/no reaction no-op). Policy and tuning file changes stay manual: a candidate writes only `agent/skills/decision-policy/policy.json` and `config/broadcast-output-tuning.json`, keeps weight deltas ≤ `0.25`, threshold deltas ≤ `0.10`, and ≤ 4 changed rules, must raise development preference agreement by ≥ `0.10` without holdout regression, must replay one unused market audit holdout with every protected metric passing, needs a clean repo plus `repo-mutation.lock`, and never commits, pushes, deploys, or activates (ADR 043) | PARTIAL | `gateReactionEvent` + `indexDiscordProviderMessages` / `resolveBroadcastByMessageId` + `applyOperatorReaction` + `loadOperatorFeedbackExamples` + `expireStaleFollowups` + `checkCandidateConfinement` / `evaluateFeedbackCandidate` / `applyFeedbackCandidate` + `scripts/lint-static.ts` allowlist check; `tests/unit/broadcast-feedback-*.test.ts`, `tests/property/broadcast-feedback-idempotency.test.ts`, `tests/redteam/broadcast-feedback-confinement.test.ts`, `tests/integration/discord-broadcast-feedback.test.ts` |

## Q — Alpha queue

| ID | Invariant | Status | Verification |
|----|-----------|--------|--------------|
| INV-Q1 | Alpha-queue items are purged only after appearing in a completed run's host-validated digest receipt; a crash between digest and purge loses no undigested message | PARTIAL | `validateAndPurgeAlphaDigest` fail-closed; purge after archive seal; present-but-invalid digests set `invalidReason` (`schema-invalid` / `run-id-mismatch`) and purge nothing — chat surfaces `alphaDigestInvalid` / `alphaPurged`; crash fixtures still thin |
| INV-Q2 | Digestion records useful content into the knowledge store (with provenance) before purge — purge never destroys the only copy of recorded knowledge. Host `state/alpha-acks/*` no-thesis tombstones satisfy the useful-record bar **at purge time** (ADR 034); after purge the archived `alpha-digest-receipt.json` is the durable record, so retention may sweep tombstones aged past `retention.alpha_ack_days` — never while the queue message still exists (ADR 044) | PARTIAL | digest `entries` require durable `state/` destination hashes; empty/malformed/`items`-shaped digests delete nothing; host ack + operator drain share tombstone helpers; `retainAlphaAckTombstones` skips pending queue messages (`tests/unit/retention.test.ts`) |

## R — Rate limits and external conduct

| ID | Invariant | Status | Verification |
|----|-----------|--------|--------------|
| INV-R1 | No client bypasses the shared rate-limit gate; per-host budgets stay below published limits (GeckoTerminal 25/min, DexScreener 200/min, Infura Core 500 credits/s with getLogs-sized serial pacing, SolanaTracker ~3/s, Birdeye ~1/s); Fomo and Pump.fun use Playwright navigation with a local daily ledger capped by `daily_navigation_budget` | PARTIAL | HTTP calls only via the gated client (lint: no raw fetch in `src/collectors/`) + unit tests `prop_inv_r1_*` / `tests/unit/rate-gate.test.ts`; Fomo and Pump usage ledgers + client tests |
| INV-R2 | Normal Twitter collectors are read-only: they never post, like, follow, repost, or DM. Host-only exceptions: (1) managed-list synchronizer may create one private list and add/remove its members after list-ID confinement; (2) engagement applicator applies the bot's like/follow/unfollow choices (FavoriteTweet / friendship ops only), with likes hard-capped at 2 per 10 minutes and like/follow/unfollow targets bound to same-run FYP. Farcaster host writes (likes, follow/unfollow) are additionally gated on Neynar `approved` signer status via `probeFarcasterSigner` (`src/collectors/farcaster/signer.ts`); pending/rejected/unavailable performs no mutation and archives an explicit gate receipt. Pump.fun host mutations are like/follow/unfollow only, bound to same-run `pump-fyp-eligible`, like-capped 2/10m, follow-capped per run. Every other mutating operation remains blocked. Research token search uses the same GET-only browser route | PARTIAL | mutation allowlists + schema hard caps + FYP and Pump eligible binding tests + FC signer gate receipts; live X/Farcaster/Pump session E2E still gated |
| INV-R3 | On HTTP 429, clients back off (honouring `Retry-After` when present) and never retry in a tight loop | ENFORCED | `gatedFetch` observes 429 onto the shared gate; `gatedFetchWithRetry` bounds attempts (default 3), retries only 429/5xx/timeout, honours capped Retry-After, exponential backoff + jitter (`prop_inv_r3_*` in `tests/unit/http.test.ts`; also covered in `tests/unit/narrative-collect.test.ts`) |
| INV-R4 | On-demand research (chat or CLI) goes through the same collector layer and rate gate as cron jobs — no direct upstream fetches from `src/chat/` | PARTIAL | `scripts/lint-static.ts` forbids raw chat fetches; Telegram research uses `src/orchestrator/research.ts` + gated Tavily/Dex collectors + host X search (`scrapeResearchTokenTwitter`); live E2E of every collector branch still open |
| INV-R5 | After an X login/challenge page, the host writes `~/.trenchcoat/x-scan/session-hold.json` and does not open X Playwright again until `tc auth twitter` clears the hold. The loop never retries the challenge page | PARTIAL | `session-hold.ts` + `assertTwitterSessionReady` + x-scan park; `tc auth twitter` clears the file; health finding `x-session-held`; tests `x-session-hold.test.ts` / `x-scan-loop.test.ts` |

## A — Advisory-only

| ID | Invariant | Status | Verification |
|----|-----------|--------|--------------|
| INV-A1 | The system holds no trade-execution keys and has no code path that signs or submits a trade (swap/transfer). Advisory-only for markets | PARTIAL | no DEX/trade SDK; Farcaster host custody under `~/.trenchcoat/farcaster/` may sign Optimism KeyGateway txs via `viem` for signer setup only (`src/collectors/farcaster/signer.ts`) — never token trades; re-check if any trade-execution feature is proposed |

## Documented exceptions

- Headful browser runs (`trench auth twitter`) execute outside the agent sandbox by
  design — they are operator-interactive and never launched by the scheduler.
  Authorized by the framework decision in TECHNICAL-SPEC.md.
- Farcaster signer create/attach (`tc auth farcaster`) may submit a KeyGateway
  transaction from host-held custody — account setup only, not market trading
  (INV-A1).
- Allowlisted Telegram operator `/agent` turns (ADR 040) run one-off Cursor CLI
  sessions with `--sandbox disabled` and `--force` against
  `TRENCHCOAT_REPO_ROOT`. Child env remains scrubbed, but sandbox-off means the
  model can still read host files (including `~/.trenchcoat/env`). Default chat
  and `/plan` stay sandboxed; ask mode never auto-promotes to agent.

## Trust assumptions

- Cursor's sandbox (Seatbelt on macOS, Landlock+seccomp on Linux) enforces
  `sandbox.json` as documented, and `.cursor/*.json` remains on Cursor's
  always-write-protected list.
- composer-2.5 via cursor-cli honours the instruction hierarchy well enough that
  INV-P3's eval, not model goodwill, is the real check.
- GeckoTerminal/DexScreener published rate limits are accurate; budgets sit below
  them to absorb error.
- The host machine and the operator's Cursor account are not compromised.

## Discord research isolation

| ID | Statement | Status | Enforcement |
|---|---|---|---|
| INV-D1 | Discord research is advisory and isolated under `~/.trenchcoat/discord/**` with Bot API replies scoped to configured guild/channels. The Discord agent session never mutates main queue, watchlist, ledger, INDEX, outbox, broadcast budget, or Telegram flows. **Bounded host exception:** after a completed Discord research run, `promoteDiscordTrackToMain` may apply a validated `track` proposal onto the main watchlist/ledger/decisions (INV-S9/S23 gate + provenance) — never from the sandboxed Discord agent itself. Discord watchlist subscribe requires the same validated track verdict (`mainTrackEligible`) plus non-hard-fail security (ADR 021) — walk/ignore tokens never get member updates. **Second bounded host exception:** exact unknown `slug:address` may enqueue the host chain-integration lane (ADR 016); Discord agents still never push/deploy. **Third bounded host exception:** idea-tracking (`tracking.json`) may enqueue tracking-origin Discord research and notify requesting members after ADR 019 qualification (INV-D3–D7); optional request `chain` is host-enforced at resolve (cross-chain fail closed, ADR 021) — never from the sandboxed agent itself. **Fourth bounded host exception:** Discord conversation turns run ask-mode, sandboxed, read-only sessions with cwd = the main agent workspace, never holding the writer lock and never mutating state (instruction integrity byte-compared per turn via `assertInstructionIntegrity`); the host may copy a validated Discord research chat report into main `agent/reports/chat/` under the main writer lock (ADR 022) | ENFORCED | separate state root + lock (`src/discord/`); isolated research runner; Discord subscribe gated on `evaluateDiscordWatchSubscribe` + `mainTrackEligible`; main promote host-only (`src/discord/promote-to-main.ts`); chain-integration intake/kick only (`src/chain-integration/`); tracking host transitions + durable match enqueue + `trackingChainAllows` (`src/discord/tracking-*.ts`); conversation integrity assert + report copy under lock (`src/discord/conversation.ts`); `DISCORD_RESEARCH_BOT_TOKEN` scrubbed from child env; webhook broadcasts remain on `DISCORD_WEBHOOK_URL` only → ADR 010 / ADR 016 / ADR 018 / ADR 019 / ADR 021 / ADR 022 |
| INV-D2 | Discord may only enqueue the host chain-integration lane. No untrusted Discord/provider text and no model-authored value may directly choose files, git commands, scanner status, push, deploy, or success copy. Host collectors + deterministic gates are authoritative | ENFORCED | typed intake + evidence snapshots + confinement + clean gate + host-rendered announce (`src/chain-integration/`) → ADR 016 |
| INV-D3 | `tracking.json` is host-owned; every mutation is a schema-validated deterministic transition under the Discord state lock, scoped to the authenticated message `(guildId,userId)`. Model output can propose only an action and allowlisted ids; it cannot choose ownership, routing, mentions, quotas, timestamps, or raw persisted state | ENFORCED | `tracking-state.ts` transitions + `store.saveTracking`; `prop_inv_d3_*` + `prop_inv_d3_tracking_store_ownership`; intake integration under lock → ADR 018 |
| INV-D4 | At every committed state, a user has at most 10 active requests per guild; expired requests are inactive for matching at `expiresAt <= now`; activation, drop, confirmation, and extension are atomic and cannot bypass the cap | ENFORCED | `countActiveForUser` / `activeMatchableRequests`; `prop_inv_d4_active_cap_never_exceeds_ten`; `prop_inv_d4_expired_requests_never_match`; unit state/expiry boundaries |
| INV-D5 | Tracking classifier and matcher sessions are composer-2.5 (config), sandboxed, fixed-prompt and path-only over untrusted snapshot envelopes. Errors/malformed output fail closed. Host allowlists every returned id and renders all Discord copy with explicit allowed mentions; untrusted/model text cannot create a mention or redirect delivery | PARTIAL | parser/allowlist/sanitize + red-team + static prompt guards ENFORCED (`prop_inv_d5_*`, `tests/redteam/discord-tracking.test.ts`); recurring live prompt-injection / semantic eval still open |
| INV-D6 | Every eligible sealed X batch, FC batch, completed ticker research, Discord research, and FOMO-origin research creates one durable match batch. Parent runs do not fail because of matching; failed/stale batches are recoverable and never silently discarded. Scan matches stay silent until host-validated ticker/CA resolution and deep research qualify the token | ENFORCED | `enqueueTrackingMatchBatch` from `run.ts` / Discord pump; stable `batchId` dedupe; worker reclaim + attempt ceiling; tokenQuery/provenance validation; integration hook/delivery tests |
| INV-D7 | A `(trackingId, chain, tokenAddress)` has one logical delivery/research trigger. Delivery and research enqueue are durable, resumable, mention-scoped, and never acknowledged as successful before state commit. Expiry notices bind exactly the elapsed plus next-48h set and are emitted once per expiry cycle. Ambiguous Discord API results are terminal without blind resend. Initial alerts require `mainTrackEligible`; three-mention `composer-2.5-fast` reconsideration may approve a later research alert (security hard-fail warned, watch/promote gates unchanged) with a 7-day blacklist on review reject | PARTIAL | delivery state machine + per-part keys + crash test + `prop_inv_d6_delivery_key_is_idempotent` / `prop_inv_d7_expiry_bundle_*` / mention-review path; Discord nonce reconciliation not available → remains PARTIAL |
| INV-D8 | Composer-2.5 correctly maps natural-language tracking/drop/extend intent and content matches at documented quality thresholds (≥95% intent accuracy, ≥90% match recall, ≤2% match FP, 100% safety) | PARTIAL | opt-in `tests/e2e/discord-tracking-model-live.test.ts`; never ENFORCED — model semantics are probabilistic |
| INV-D9 | Discord conversation is fail-closed and host-mediated: the addressing gate defaults to silence (deterministic pre-filters; classifier output parsed strictly, any error → not addressed); member text reaches models only as untrusted snapshot content or delimited conversational input; model output can only propose research subjects that the host re-validates against the chain/CA/ticker grammar (bounded per turn) — never file paths, mentions, delivery targets, or state; all replies are path-scrubbed and sent with empty allowed-mentions; synthesis replies cannot enqueue further research | ENFORCED | `conversation-intent.ts` / `conversation.ts` parsers + validators; `prop_inv_d9_gate_fails_closed_*`, `prop_inv_d9_subjects_host_validated`, `prop_inv_d9_reply_never_leaks_workspace_paths`; `tests/redteam/discord-conversation.test.ts`; lint-static raw-fetch ban on `src/discord/conversation*` → ADR 022 |

## S — Repository mutation (chain registry)

| ID | Statement | Status | Enforcement |
|---|---|---|---|
| INV-S26 | Automated chain integration may only add an additive `chains/<slug>.json` (+ generated registry + new tests + narrow docs), publish via fast-forward to `origin/main` after clean gates, claim success only after deploy health, and roll back via normal revert + `runtime.prev`. INV-S24 remains harness decision-policy only | PARTIAL | `confinement.ts` + `gates.ts` + `publish.ts` + deploy health; unit/red-team coverage in progress |

## S — Repository mutation (incident remediation)

| ID | Statement | Status | Enforcement |
|---|---|---|---|
| INV-S27 | Host-owned incident remediation uses sanitized/path-only evidence prompts; stable dedupe + durable cursors/journals with idempotent side effects; weekly policy harness semantics/allowlist unchanged; low-risk path confinement and exact-hash Telegram approval for high-risk; absolute-deny surfaces cannot be overridden; independent pre/post-build reviews plus non-waivable worktree `pnpm install --frozen-lockfile` + secret/typecheck/lint/`test:all` gates (sibling worktrees never inherit main `node_modules`); pre-review `revise` auto re-enters propose with `priorPreReviewPath` up to `max_pre_review_revises` then fails closed for the operator (`reject` fails on first decision); ff-only serialized publish, clean provenance, deploy via OS install script (`install-systemd.sh` on Linux / `install-launchd.sh` on macOS), deploy health, revert/runtime rollback; models never mutate production runtime state directly. Host ignores log/health/skip incidents when every mapped job is healthy in the current snapshot (`already-recovered`), ignores in-SLA `outcomes-settle` incomplete-run findings (age under 24h) and bare `incomplete runs=N` counts, and does not reopen a terminal fingerprint until a mapped job is degraded; log prompts receive a bounded `evidence-log.txt` snapshot; leftover post-build artifacts are removed before diagnose. Post-deploy phases include source-recovery and claim-audit gates (INV-S28) before completion. **Discord suggestions:** passive conversation-thread intake may enqueue `discord-suggestion` incidents; messages are untrusted envelopes (path-only prompts); Stage A host prefilters + host worth-building gate fail closed; classifier may only select host-listed thread/message ids; admission needs both a request word and a product-surface word (a bare mention admits nothing); a formed suggestion must carry `symptom`, `intendedBehavior`, and one to five measurable `acceptanceCriteria` — a missing intended behavior downgrades to `forming` and every other gap is `classifier-failed`; disagreement yields one host-recorded recommendation with `alternativesConsidered` + `recommendationRationale` (never a skip); `forming` digests are host-owned, bounded, and collapse to one count in operator digests; the only Discord write is one host-rendered clarifying question per suggestion entry, on the first `forming` round, with links, mentions, and secrets stripped and a fixed fallback when the classifier text is not a question; deny-surface suggestions never escalate; risk gates unchanged | PARTIAL | `src/remediation/*` + `src/harness/deploy.ts` `resolveHostInstallScript` + `src/lib/worktree-deps.ts` + ADR 017 + ADR 025 + `prop_inv_s27_*` |
| INV-S28 | A market-affecting remediation cannot complete, mutate corrective state, or emit a `finding.correction` until the deployed commit has produced the configured fresh healthy source observations and a sealed revalidation run; automatic invalidation requires allowlisted post-fix citations, claim-specific deterministic contradiction where available, unanimous evaluator+reviewer agreement, and zero reviewer uncertainty; supersession is append-only via host-validated paths; public corrections are destination-scoped (only claims originally delivered there), one stable correction per incident/destination, HMAC-authenticated, idempotent, and bypass worthiness as integrity notices — never the policy harness (INV-S24). Integrity hold is released on `attention-required`, operator `fail`, and `completed`; when every affected source kind is absent from the source-health ledger the recovery wait is skipped and the incident escalates instead of blocking broadcasts indefinitely | PARTIAL | `source-health.ts` + `market-claims.ts` + `post-fix-audit.ts` + `revalidate.ts` + `reconcile.ts` + `correction.ts` + integrity hold + `finding.correction` router contract + `prop_inv_s28_*` |
| INV-S29 | Runner discovery and tracked-wallet convergence are deterministic and idempotent: pool qualification, top-N earliest verified buyers, multi-runner recurrence, anti-automation rejects, candidate caps, and ≥4 unique event-time-`tracking` wallet convergence use host maths only; retries/reorders cannot duplicate candidates, alerts, or research enqueues for the same stable IDs | PARTIAL | `src/wallets/runner-discovery.ts`, `src/wallets/convergence.ts`, `wallet-runner-discovery` / `wallet-convergence` orchestrators; unit + `tests/property/wallet-runner.test.ts` + crash cursors; live canary gates in `ops/runner-wallet-canary.md` / ADR 020 still open |

- Changes touching an enforcement site re-check the affected rows in the same PR.
- Name tests after the invariant they check (`prop_inv_i4_*`) so coverage is greppable.
- Status upgrades land with the fix, citing the finding that opened the gap.
- Each implementation milestone must flip its module's rows from GAP before the
  module is considered done.
